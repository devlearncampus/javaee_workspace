<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "mybatis-3-mapper.dtd">
<mapper namespace="Comment">
	
	
	<!-- 댓글 등록 -->
	<insert id="insert" parameterType="Comment">
		insert into comment(msg, user) values(#{msg}, #{user})
	</insert>
	
</mapper>














